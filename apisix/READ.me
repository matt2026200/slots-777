[ 客户端群 (CC 攻击/正常请求) ]
           │
     ┌─────┴─────┐ (通过负载均衡如 LVS / F5 轮询)
     ▼           ▼
┌─────────┐ ┌─────────┐
│ APISIX  │ │ APISIX  │  <-- 节点 1 & 节点 2
│ 节点 1  │ │ 节点 2  │
└─────────┘ └─────────┘
     │           │
     │ 1. 验证 HMAC 并注入 ctx.consumer_name
     │ 2. limit-req 插件触发
     ▼           ▼
┌─────────────────────┐
│  中心化 Redis 集群   │  <-- 所有节点原子性读写同一个 Key 计数
└─────────────────────┘
